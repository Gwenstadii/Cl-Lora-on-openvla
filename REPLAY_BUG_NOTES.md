# 原型回放实验问题记录（REPLAY BUG NOTES）

> 目的：记录 CL-LoRA + 原型回放（RoboTwin）实验中发现的问题与结论，防止遗忘。
> 更新时间：2026-08（实验进行中，随时补充）

## 1. v39r 的实验定性问题（重要！）

**现象**：v39r（回放版 BCD）训练完成后，评估 A=0.9、B=0（C/D 当时未完成）。

**关键发现**：v39r 启动时服务器代码里包含 **cd003ff**（"Stage>1 无条件冻结 FiLM"），
直到 **f68d28b** 才把冻结改成默认关闭（`--freeze_film_stage2 False`）。
日志确认（train_v39_replay_BCD.log）：

```
[FiLM] Stage>1: vision_backbone (含 FiLM) 已冻结, 视觉特征恒定
# trainable params in vla.vision_backbone (frozen): 0
```

**结论**：v39r 实际是 **冻结 FiLM + 原型回放**，而不是原计划（漂移 FiLM + 回放）。
- A=0.9 符合"冻结保视觉（v39f 无回放 A=0.7）+ 回放强化"的组合预期
- **"漂移 FiLM + 回放"实验尚未真正跑过**——如需该配置，必须显式 `--freeze_film_stage2 False`

## 2. B=0 / B/C/D 全崩（回放+KD 超载压垮新任务）——已确认

**最终结论（2026-08 评估确认）**：v39r 完整结果：

| checkpoint | A | B | C | D |
|---|---|---|---|---|
| v39r-C | 0.81 | 0.00 | 0.00 | — |
| v39r-D | 0.90 | — | 0.00 | 0.02 |

**决定性证据**：C 用自己 checkpoint 评估也是 0、D 自评 0.02 —— **新任务根本没学好**。
根因不是 bank 复制（md5 完好），是**训练强度三倍超载**：

- v39r 配置: replay_every=1（每步回放）+ replay_weight=1.0 + lambda_kd=1.0（每步 KD）
- B 阶段每步梯度构成: task(1份) : replay(1份) : KD(1份, 把新任务拉向旧teacher行为)
- ⇒ B 特定层 2/3 梯度在学 A 的行为 → rank16×8层容量被灌爆 → B/C/D 全崩
- A 是 stage1 学的 + 回放强化 → 0.9（幸存）

LIBERO 同样 1:1:1 没崩（7D 单臂简单）；RoboTwin 14D 双臂直接压垮。
**这是强度问题不是机制错误。**

**v2 修复（run_v39_replay_BCD.sh 已改，run_id rt_v39r2）**：
- `--freeze_film_stage2 False`：显式回到"漂移 FiLM + 回放"原叙事（v39r 意外冻结）
- replay_every 1→4、replay_weight 1.0→0.5
- lambda_kd 1.0→0.2

**诊断方法备忘**：新任务用自己 ckpt 自评（eval_task_id=N, 10episode）——低 = 学习被压垮（强度问题）；高 = bank/评估链问题。

## 3. 评估加速注意事项

- 改 step_lim / num_open_loop_steps 会破坏与历史结果（0.98/0.04/0.42…）的可比性，**不要改**
- 零代价加速：`gpus="6,6,7,7"`（4 worker，每卡 2 进程，显存 ~30GB/卡足够）
- 训练进程占 CPU 时评估显著变慢——先确认训练结束再评估

## 4. 当前实验矩阵（2026-08 状态）

| 支线 | 配置 | 结果（D ckpt 全任务） |
|---|---|---|
| v39 漂移版（无回放） | FiLM 训练 + block_scale 漂移 | A=0, B=0.42, C=0.02, D=0.82 |
| v39b2 漂移版（无回放） | FiLM lr×0.2 + block_scale 冻结 | A=0, B=0.24, C=0, D=0.8 |
| v39f v1（冻结版） | FiLM 冻结 + block_scale 漂移 | A=0.7, B=0.72, C(Dckpt)=0, C(Cckpt)=高 |
| v39f2（冻结版 v2） | FiLM + block_scale 全冻结 | 待评估 |
| v39r2c（回放 v2 + 冻结FiLM 重训D） | 冻结 FiLM + buffers A×2+B×1+C×3 | A=0.50, B=0.64, C=0, D=0.79（含 proprio bug） |
| v39r2d（回放 + proprio 修复重训D） | 冻结 FiLM + proprio 从 prev 加载 | A=0.62, B=0.88, C=0.88, D=0.80 ← proprio bug 修复后 C 恢复（定稿） |
| v39b3（无回放, proprio 修复, A冻结） | 漂移FiLM lr0.2 + A冻结 + proprio修复 | A=0.26, B=0.88, C=0.72, D=0.82 |
| v39b4（无回放, proprio 修复, A漂移） | FiLM冻结 + freeze_specific_a=False | A=0, B=0.57, C=0, D=0.85 |
| **普通 LoRA（标准 PEFT 顺序微调，对齐版）** | lora_scope=cl + FiLM恒等 + proprio共享 + 无bank/无回放 | **A=0, B=0, C=0, D=0.82** ← 灾难性遗忘最底层基线 |
| 锚定正则（保底，未跑） | film_anchor_reg λ | — |
| FiLM 进 bank（已完成） | bank 存 vision_backbone + film_gamma | γ=1: A/C≈0.9；γ∈[0.1,0.8]: C=0（全或无） |

**三支线对比（定稿实验，详见 方法论.md §10.9）**：

| 支线 | A | B | C | D | 旧任务均值 |
|---|---|---|---|---|---|
| 普通 LoRA（无防遗忘） | 0.00 | 0.00 | 0.00 | 0.82 | 0.00 |
| CL-LoRA 无回放（v39b4：FiLM冻结 + specific-A漂移） | 0.00 | 0.57 | 0.00 | 0.85 | 0.19 |
| CL-LoRA 无回放（v39b3：FiLM慢漂移 + specific-A冻结） | 0.26 | 0.88 | 0.72 | 0.82 | 0.62 |
| CL-LoRA+原型回放（v39r2d） | 0.62 | 0.88 | 0.88 | 0.80 | 0.79 |

> 注 1：v39b3（漂移 FiLM lr0.2）与 v39r2d（冻结 FiLM+回放+KD）差两个变量，结论按"组合效果"表述。
> 注 2（**b3 vs b4 的关键结论**）：两版仅"FiLM 漂移↔冻结"与"specific-A 冻结↔漂移"成对交换 —— 结果显示
> **specific-A 漂移（LLM 侧 A/B 配对错位）是毁灭性的**（A/C 归零，0/0.57/0），**FiLM 慢漂移是温和的**（0.26/0.88/0.72）。
> 即：防遗忘第一优先级 = **A/B 配对完整（A 冻结）**；FiLM 恒定是第二优先级（重要非充分）。与 LIBERO V47 结论一致。
> 注 3（**FiLM 旋钮失效，负结果 v39b6**）：`film_lr_scale` 0.2→1.0 → 结果 0/0.14/0/**0.25**（连新任务 D 都崩）。
> 诊断：lr0.2× 单轮已漂 65%、lr1.0× 累积漂移 166%（v39 同 lr = 164%，量级一致）→ 无可用中间档；
> "FiLM 全速漂移+其余全冻"使新任务训练失稳（全部适应压力落在漂移 FiLM 上）→ 这是 v39r2d 冻结 FiLM 的反面证据。
> **同时修正**：此前"γ 全或无 / C 对 FiLM 零容忍"的结论被 proprio bug 污染（C 恒 0 主因是 proprio 错位；
> b3 实测 FiLM 漂 65% 时 C 仍 0.72 → C 对 FiLM 高度耐受）。
> **最终**：中间残留态不可稳定获得（retention 对关键组件漂移呈开关式响应）→ 无回放基线二选一：b3 或 b4。

**实验配对关系（无回放 ↔ 回放，同配置才算干净对照）**：

| 无回放 | 回放 | 配对状态 |
|---|---|---|
| v39（漂移FiLM lr1.0, bug）0-0.42-0.02-0.82 | **v39r2**（lr1.0+回放降载）0.69-0.80-0-0.75 | ✅ 同 FiLM lr（均含 proprio bug，同环境） |
| v39b2（lr0.2, bug）0-0.24-0-0.86 | **无**（lr0.2+回放从未跑） | ❌ 空洞——b2 无同配置回放配对 |
| v39b3（lr0.2+修复）0.26-0.88-0.72-0.82 | 无（修复版漂移+回放未跑 = v39r2e 待跑） | ❌ 空洞 |
| v39b4（A漂移+修复）0-0.57-0-0.85 | 无（False+回放未跑 = v39r2f 待跑） | ❌ 空洞 |
| **无（冻结FiLM+True+修复+无回放未跑 = v39b5 待跑）** | **v39r2d**（冻结FiLM+True+修复+回放）0.62-0.88-0.88-0.80 | ❌ 空洞——**v39r2d 无严格无回放配对** |

**v39r2d 补充说明**：只重训了 D（从 v39r2 链的 C 起），B/C bank 来自 v39r2（含 proprio bug 训练，但 v39r2d 的 proprio=从 C 加载 → 评估组合自洽 → C=0.88 成立）；冻结 FiLM 下 D 训练 FiLM 恒等 C-FiLM。其严格对照（同配置无回放）需补跑：冻结 FiLM + freeze_specific_a=True + proprio 修复 + 无回放 BCD。

**FiLM 恢复的"全或无"实证**：γ=0.1~0.8 下 C 全 0，γ=1 才活——参数插值给不了 0.2-0.5 中间残留。
**⚠️ v39/v39b2/v39f/v39r/v39r2/v39r2b/v39r2c 数字均含 proprio 随机投影 bug（见 §5）；v39b3/v39b4/v39r2d 为修复后干净数字。**

## 5. proprio_projector 随机初始化 bug（C 类任务归零的隐藏根因，已修复 33202d9）

**现象**：v39r2c（冻结 FiLM，task_3_bank / vision_backbone 与 C 自评 **md5 完全一致**）评 C 仍 = 0，而 C 自评 0.95——文件级对比暴露唯一残留差异：**proprio_projector**。

**根因**：train_cl_lora.py 每次训练**全新随机初始化** ProprioProjector，从不从上一阶段加载（对比 FiLM 是从 prev 加载的）。各 stage 的 proprio 投影 = 不同随机矩阵：
- Stage N 训练：模型在随机投影 P_N 下学习（P_N 从不训练、仅被"适应"）
- 评估旧任务 K（用 stage M>K 的 ckpt）：加载 P_M ≠ P_K → proprio 输入错位
- 依赖 proprio 的视觉敏感任务（C 精确堆叠）→ 归零；A/B 容错大只降幅

**历史影响（重要）**：bug 存在于所有 stage 2+ 训练，"任何 D ckpt 评 C=0、C 自己 ckpt 评 C 高"
的现象可能**部分归因于 proprio 而非 FiLM/block_scale/回放**：
- v39f 的 C=0（当时归因 block_scale 漂移）需重审
- "漂移 FiLM + 回放救不回 C"（v39r2b 结论）需重审——proprio 修复后可能本来就该恢复
- 无回放基线（v39b2 的 A=0/C=0）可能被 proprio 因素夸大

**修复（33202d9）**：stage 2+ 时 proprio_projector 从 previous_checkpoint_dir 加载
（全任务共享 stage1 投影，与 FiLM 同机制）。日志标志：`[Proprio] Loaded proprio_projector from ...`。

**v39r2d（修复版重训 D）**：冻结 FiLM + proprio 修复 → C 恢复（C=0.88）。
**已验证（b3）**：漂移 FiLM + proprio 修复 → C 也恢复（0.72）⇒ **冻结 FiLM 非必要**。

### 5.1 遗忘归因链（控制变量推理，最终结论）

> 回答"v39b2 的 0-0.24-0-0.86 是不是 FiLM 漂移造成的"——**不是**。四条支线逐步只改一个变量：

| 支线 | FiLM | specific-A | proprio | 结果 A-B-C-D | 与上一条的唯一差异 → 结论 |
|---|---|---|---|---|---|
| v39b2 | 漂移 0.2× | True 冻结 | **bug** | 0-0.24-0-0.86 | 基准（含隐性 proprio bug） |
| **v39b3** | 漂移 0.2× | True 冻结 | **修复** | **0.26-0.88-0.72-0.82** | 只修 proprio → B +0.64 / C +0.72 / A +0.26 ⇒ **v39b2 崩塌主因 = proprio，非 FiLM** |
| **v39b6** | 漂移 **1.0×** | True 冻结 | 修复 | 0.27-0.82-?-0.875 | FiLM 漂移强 5 倍 → 几乎不变 ⇒ **FiLM 漂移强度无影响** |
| **v39b4** | **冻结** | **False 漂移** | 修复 | **0-0.57-0-0.85** | A 由冻结改漂移 → A/C 归零 ⇒ **specific-A 漂移是毁灭性的** |

**遗忘主因排序**：
```
proprio_projector 随机错位（隐形，只污染旧任务评估）
  ≈ specific-A 漂移（LLM 侧 A/B 配对错位，结构性失效）
  ≫ FiLM 漂移（0.2× vs 1.0× 无差异，几乎可忽略）
```
**直接推论**：
1. **v39b2 与 b4 不可直接对比**（差 proprio 变量）——"b2 崩 ⇒ FiLM 漂移有害"是错误归因；
2. 防遗忘第一优先级 = **freeze_specific_a=True（A/B 配对完整）+ proprio 跨 stage 一致**；
3. FiLM 处理（冻结 / 漂移 / bank 恢复）对 retention 影响极小 ⇒ 此前围绕 FiLM 的调参方向（γ 插值、scope、lr 梯度、锚定）**本就不是主要矛盾**；
4. 需要"含 bug 数字"的结论时先看 §4 末行标注：v39 / v39b2 / v39f / v39r / v39r2 / v39r2b / v39r2c 均不可用于 FiLM 归因。

## 7. 三支线配置对比（普通 LoRA / CL-LoRA 无回放 / CL-LoRA+原型回放）

> 详见 `方法论.md` §10.8。核心：方法变量 = LLM 适配结构 + 防遗忘机制；其余组件尽量对齐。

| 维度 | 普通 LoRA（基线） | CL-LoRA 无回放（v39b4） | CL-LoRA+原型回放（v39r2d） |
|---|---|---|---|
| 入口/脚本 | finetune.py；run_loraA.sh + run_lora_BCD_afterA.sh | train_cl_lora.py；run_v39b4_baseline_BCD.sh | run_v39r2d_stageD.sh |
| LLM 注入 | L16-31 × 7 proj（PEFT 正则，rank16） | 同层同模块（CL-LoRA 注入） | 同左 |
| LoRA 结构 | 标准 PEFT（B@A，随机 A，无门控） | shared(L16-23): 正交A冻结+B冻结；specific(L24-31): A 漂移(False)+B 重训+block_scale | 同左 |
| action_head | 全参数训练 | CL-LoRA 注入 + bank | 同左 |
| 视觉/投影器 | 冻结 | 冻结 | 冻结 |
| FiLM | **恒等**（置零冻结，无调制） | A 训练→stage≥2 冻结 | 同左 |
| proprio | 冻结；**从 prev 加载**（已对齐） | 不训练 + 从 prev 加载 | 同左 |
| task bank | 无 | 有 | 有 |
| 防遗忘机制 | 无（顺序覆盖） | 冻结共享 + bank 隔离 | 冻结 + bank + 回放(every4/w0.5, A×2+B×1+C×3) + KD(λ0.2) |
| 评估 | eval_task_id=0 | eval_task_id=1..4（bank） | 同左 |
| 结果 A-B-C-D | **0-0-0-0.82**（A 自评 0.9167） | 0-0.57-0-0.85 | 0.62-0.88-0.88-0.80（**含 KD**） |

**普通 LoRA 修复链（本机→git 已推送）**：
- `lora_scope cl`（f07d272/741309d/3c01943）：PEFT 正则对齐 L16-31 同模块 + proprio 冻结
- FiLM 前缀兼容（ff37e09）→ FiLM 恒等化（2a92a05）：scale/shift 是 nn.Linear 随机初始化（非恒等！），两侧置零保证训练/评估一致
- proprio 从 prev 加载（对齐 CL 33202d9，本轮）：避免每 stage 随机投影错位

## 8. CL 评估前缀剥离回归（118f974，曾致 A=0 假象）

**现象**：v39u 首次 A 自评 = 0（该 ckpt 此前 0.98）；B 自评仅 0.5。
**根因**：为兼容 finetune.py 加的"剥 `vision_backbone.` 前缀"逻辑放在 CL/普通 LoRA **共用位置**
→ CL 的 key 被剥 → `missing=889, unexpected=889` 全量错位 → FiLM（**随机初始化，非恒等**）→ 视觉特征被破坏 → 全崩。
**关键事实**：该前缀来自 **FiLM wrapper 的内部属性名**（`FiLMedPrismaticVisionBackbone.vision_backbone`），
训练保存与评估模型两侧**本来就一致**——不需要任何剥离。
**影响面**：ff37e09 之后的所有 CL 评估；**b6 首次评估（0/0.14/0/0.25）被污染**，重评后为 **0.27/0.82/待重评/0.875**。
**修复**：删除剥离；并在加载后打印 `missing/unexpected` 计数（静默失败必须可见）。
**教训**：`strict=False` 是隐形杀手——diff 计数、核对 key 样例、任何"加载成功但结果异常"先查它。

## 9. v39b6（FiLM 漂移 1.0×）——重评后的真实结论

| | FiLM lr | A | B | C | D |
|---|---|---|---|---|---|
| v39b3 | 0.2× | 0.26 | 0.88 | 0.72 | 0.82 |
| **v39b6（重评）** | **1.0×** | **0.27** | **0.82** | 待重评 | **0.875** |

**新结论**：**FiLM 漂移强度几乎不影响 retention**（b3 ≈ b6）——"FiLM 是 A/C 归零主因"的旧叙事整体降级；
决定 retention 的是 **LLM 侧 A/B 配对（specific-A 是否冻结）与 proprio**。同时 `film_lr_scale` 不能当 retention 调节器
（漂移快速饱和：0.2× 单轮已 65%、1.0× 累积 166%），"中间态"仍不可得。
**已撤销**："FiLM 全速漂移伤新任务（D=0.25）"——重评 D=0.875，原值是评估链回归 bug 的假象。

## 10. v39u（CL-LoRA + 普通回放 uniform，**纯回放无蒸馏**）——当前主线

**方法定义更正**：**方法 = CL-LoRA + 纯回放（无蒸馏）**；v39r2d（λ_kd=0.2）属"回放+KD"配置，非方法本体。

**配置速查（核对脚本，四维）**：

| 支线 | FiLM | specific-A | 回放 | KD | 结果 |
|---|---|---|---|---|---|
| v39b4 | 冻结 | **False 漂移** | 无 | 无 | 0-0.57-0-0.85 |
| v39b3 / b6 | 漂移 0.2× / 1.0× | True 冻结 | 无 | 无 | 0.26-0.88-0.72-0.82 / 0.27-0.82-?-0.875 |
| **v39r2d** | **B/C 漂移 → D 冻结（混合/脏，仅 D 冻）** | **True 冻结** | 原型 every4/w0.5+加权 | **有 λ0.2** | 0.62-0.88-0.88-0.80 |
| **v39u** | **全程冻结** | True 冻结 | uniform every4/w0.5+加权 | **无** | 进行中 |
| **v39v（待跑）** | **全程冻结** | True 冻结 | **原型** every4/w0.5+加权 | 无 | 未跑 |
| **v39r3（待跑）** | **全程冻结** | **False 漂移** | **原型** every4/w0.5+加权 | 无 | 未跑 |
| 普通 LoRA | 恒等 | — | 无 | 无 | 0-0-0-0.82 |

### 10.1 缺口核对（2026-08，逐脚本核对）——两个空格

**问题**："冻结 FiLM + specific-A 漂移 + 回放"做过吗？**没有**。核对依据：

1. 所有回放支线（v39r/r2/r2b/r2c/r2d/u）脚本都是 `--freeze_specific_a True`；
2. 唯一 `--freeze_specific_a False` 的支线是 **v39b4**，而 b4 是 `--use_replay False` → **回放 ∩ A 漂移 = ∅**；
3. **v39r2d 是脏版**：`run_v39r2d_stageD.sh` 的 `--freeze_film_stage2 True` 只作用于它重训的 **stage D**（起点/teacher = `rt_v39r2_taskC`，B/C bank 来自 v39r2 链：漂移 FiLM + proprio bug 期训练）⇒ 它的准确标签是"**仅 D 阶段冻结 FiLM + 有 KD**"，不能当作"冻结 FiLM + 回放"的证据。

**更意外的发现**：**"全程冻结 FiLM + A 冻结 + 原型回放 + 无 KD"（方法本体的干净配置）同样从未跑过**——v39u 是它的 uniform 版，v39r2d 是它的脏版。⇒ 原型回放行的两个格子都是空白：

| 冻结 FiLM + 无 KD | specific-A 冻结 | specific-A 漂移 |
|---|---|---|
| 无回放 | b5（未跑, 可选） | **b4 = 0-0.57-0-0.85** ✅ |
| **原型回放** | **v39v ← 空白（方法本体）** | **v39r3 ← 空白（回放能否替代 A 冻结）** |
| uniform 回放 | v39u（在跑） | v39w（可选） |

**补跑脚本**：`run_v39v_prototype_replay.sh`（`FREEZE_SPECIFIC_A=True`→v39v / `False`→v39r3，A ckpt 与原型 buffers 全复用，无需重建）+ `run_v39_prototype_2x2.sh`（两条串行，带 B 自评门禁）。
**预期对照**：v39r3 vs b4 = 干净单变量（加原型回放）；v39r3 vs v39v = 干净单变量（freeze_specific_a）；v39v vs v39u = 干净单变量（prototype vs uniform）。
**机制**：stage2+ 只 reinit specific-B + block_scale，specific-A 继续漂移；bank 不存 A ⇒ b4 的 A/C 归零。回放梯度会流进 A ⇒ v39r3 测的是"回放能否把 A 拉回旧任务兼容值"。

### 10.2 每任务 buffer 样本数为什么不同（预算公平性核对）

**不是 bug，是设计的结果**：固定的是"**每任务 `--num-episodes 10` + 每运动段 `--top-k 3`**"，
样本数 = Σ_episode Σ_segment min(top_k, 可选帧数) ⇒ **随每集运动分段数（任务动作结构）变化**。
- 分段 = 臂/夹爪差分阈值(0.05/0.1) → 运动模式 run-length → 短段(<5帧)合并；段数越多的任务 buffer 越大；
- `T < kinematic_window + min_segment_frames` 的极短 episode 被 `continue` 跳过 → 占用 episode 名额但不产样本（`diagnostics.jsonl` 行数可能 < 10）；
- `top_k` 是上限：inlier 不足或 `temporal_min_gap` 约束下可能选不满（`_select_coverage_representatives`）。

**样本数不进梯度权重（三点公平性）**：
1. uniform buffer 用 `--match-budget-buffer-dir` **逐任务**匹配原型样本数 → 消融干净；
2. 每个 buffer 目录 = 独立 DataLoader，`replay_sample_strategy="round_robin"`（train_cl_lora.py:517）轮流取 batch ⇒ **每目录机会均等**，加权只靠目录重复（A×2+B×1+C×3 → 2/6:1/6:3/6）；
3. every4 × weight0.5 ⇒ replay 占总梯度 ~1/8，与 buffer 大小无关。

**统计工具**：`python vla-scripts/analyze_replay_buffers.py --roots $LOGS_ROOT/replay_buffers $LOGS_ROOT/replay_buffers_uniform --weights taskA=2,taskB=1,taskC=3`
（读 `meta.json` 的 `saved_replay_samples`/`source_frames`/`compression_ratio`/`num_segments` + `diagnostics.jsonl` 的每集段数，输出各任务样本数、压缩率、段/ep、样本/段，并核对 prototype vs uniform 是否逐任务等额）

### 10.3 v39r3（A 漂移 + 原型回放）——旧任务 C 保留率低 ⇒ **回放不能替代 A 冻结**（数字待补）

**结果（2026-08 实测）**：**A=0，B≈0.80，C≈0.20**（D 未评，用户判断不需继续）。
与 b4（同配置无回放：0 / 0.57 / 0 / 0.85）单变量对比 ⇒ **回放的真实作用首次被干净量化**：

| 任务 | A 漂移步数 | b4（无回放） | v39r3（+原型回放） | 回放增益 |
|---|---|---|---|---|
| A（最早，漂 3 轮） | 3 | 0 | **0** | **+0** |
| B（漂 2 轮） | 2 | 0.57 | **≈0.80** | **+0.23** |
| C（漂 1 轮） | 1 | 0 | **≈0.20** | **+0.20** |

**核心结论（两条，都可入论文）**：
1. **回放的收益 ∝ 1/漂移程度**：漂移越轻的任务回报越高（B +0.23、C +0.20），漂移到 A 那种程度就**完全无效**（+0）——回放能补偿"轻度错位"，不能撤销"结构性错位"（A_final 与 A_1 的距离已经太大，bank 里 B_A 无论如何配不回来）。
2. **A 漂移 ⇒ A 保留恒为 0**：v39 / v39b2 / b4 / r3 四次独立配置（FiLM 冻结或漂移 0.2×/1.0×、有/无回放、有/无 KD）**全部 A=0** ⇒ A 漂移是 A 保留归零的**充分条件**，且回放不能补偿 ⇒ **specific-A 冻结是"高保留"的必要条件**。

**机制（三条，主因是第 1 条）**：
1. **结构性主因：bank 不存 A，而 A 在漂**。`save_task_bank`(cl_lora.py:283) 只存 specific `lora_b` + `block_scale`（+ FiLM），`load_task_bank` 也只恢复这两样。评估任务 C 时算的是 `W + s·g_C·B_C·A_final`，训练时是 `W + s·g_C·B_C·A_3` ⇒ A 一离开 A_3，B_C 立即失配，D 阶段 40k 步继续放大误差，**没有任何机制能把它拉回**。
2. **为什么偏偏 C 塌**：C=stack_bowls_two 需精确接触相位与毫米级对位，是全系列对扰动最敏感的任务（proprio 错位时也是 C=0）→ 表征错位"开关式"归零；B=grab_roller 容错大（b4 里 B=0.57 而 A=0）。
3. **回放为何无效（本次实验的核心答案）**：① **剂量** —— replay 梯度恒占 ~1/8（every4 × w0.5），task loss 每步都在推 A，1:8 拉锯由 task loss 主导；② **方向不对** —— replay 监督的是"旧状态 s → 旧动作 a"的**输出**，并不显式恢复 A_K，而失效发生在"B_K 与 A 的配对"；A 可在不明显抬高 replay loss 的前提下沿新任务方向走（buffer 只覆盖有限状态点，泛化不到整个状态空间）。
⇒ **结论：回放 = 输出层面的弱正则；A 冻结 = 参数层面的结构保护，回放不能替代 A 冻结**（"为什么必须 freeze_specific_a=True"的最强正面证据）。b4（0-0.57-0-0.85，无回放）与 r3（有回放）构成干净单变量对照。

**排除干扰项（先看这三行日志）**：deploy 的 `missing/unexpected` 计数（应为 0）、`[Proprio] Loaded proprio_projector from ...`、`[TaskBank] Loaded action_head LoRA from bank`。
**漂移量化工具**：`python vla-scripts/analyze_cl_lora_drift.py --ckpts <A ckpt> <r3 B> <r3 C> <r3 D> --names A B C D`（specific-A 逐层 cos / 相对 L2，判定 bank 恢复误差下界）。
**连带结论**：v39w（uniform + A 漂移）预期同样崩，可省；**v39v（A 冻结 + 全程冻 FiLM + 原型回放 + 无 KD）仍需跑**（方法本体，与 r3 仅差 freeze_specific_a）。
**r3 指向的新设计方向（待评估）**：把 **specific-A 也存进 bank**（每任务 A_K 快照）→ 评估时 A_K 与 B_K 同时恢复，函数完全复原（保留率≈自评）；即"冻结 A 的正交保护" vs "参数隔离快照"两种范式对比（代价：bank 体积 ↑，`save/load_task_bank` 各加几行）。

### 10.4 "CL-LoRA 无回放"的真实强度 = b3（不是 b4）：回放价值需用同 FiLM 设置的孪生臂证明

**事实核对**：`run_v39b3_baseline_BCD.sh` 明写 `--use_kd False --use_replay False`（+ 漂移 FiLM `film_lr_scale=0.2` + `freeze_specific_a True` + proprio 修复）
⇒ **b3 本身就是"该配置的 CL-LoRA 无回放"，不是还没做**；**B=0.88 / C=0.72 来自结构保护**（冻结 shared A+B + 冻结 specific-A + bank 隔离 + block_scale 冻结），**不是回放贡献**。

**战略风险（必须先排掉）**：
1. 拿 b4（A 漂移）当"无回放基线"= 用最弱配置陪衬 → 选择性偏差质疑（b3/b4 只差 `freeze_specific_a`，两者都该报告）；
2. b3 与现有回放臂（v39r2d/v39v/v39u）**FiLM 设置不同 ⇒ ≥2 变量**，无法单变量归因；
3. **γ=1 口径下 b3 的 A/C≈0.9**（bank 含任务 FiLM 时可恢复）可能反超 v39r2d 的 A=0.62 ⇒"回放必要性"有被自身基线推翻的风险，必须在投稿前用实验排掉。

**解法（脚本已推 `run_v39r5_driftfilm_replay.sh`）**：v39r5 = **b3 配置 + 原型回放 every4/w0.5 + 无 KD + proprio 修复**，唯一差异 = 回放开关。
- v39r5 ≫ b3 ⇒ 回放确有边际价值，b3 可作正式基线；
- v39r5 ≈ b3 ⇒ **CL-LoRA 结构隔离才是主机制**，叙事改为"结构隔离为主 + 回放增强 X"。
**零成本补数**：b3 的 D ckpt 用 **γ=1** 重评（只评估，不训练）→ 填"bank 含 FiLM 恢复"口径；先查 `task_1_bank.pt` 是否含 `vision_backbone` key。

#### 10.4.1 为什么回放的提升这么有限、A 甚至 +0（机制定论）

**前提核对**：b4 与 r3 **确实同配置**（b4: `--use_replay False --use_kd False --freeze_specific_a False --freeze_film_stage2 True`；r3 = 同 + `--use_replay True` 原型 every4/w0.5 + D 阶段 A×2+B×1+C×3）⇒ 唯一差异 = 回放开关。

**三条机制（按重要性）**：

1. **最关键：评估时真正使用的那条路径 `(B_K, A)` 从未被训练过。**
   stage 2+ 每阶段开头 `reinit_bank_for_new_task` 把 specific-B **清零重建**，训练中模型里装的永远是"当前任务的 B_N"；任务 K 的 B_K 躺在 `task_K_bank.pt` 里**从不参与训练**。所以回放旧任务数据时，梯度落在 `(B_N, A)` 上，而**评估用的是 `(B_K, A_final)`** ⇒ 没有任何一次更新直接优化目标函数，回放只能"间接"让 A 停在更兼容旧 B_K 的区域。剂量越大（A 经历 3 个阶段）该间接信号越失效 ⇒ A 增益恒为 0。

2. **剂量 × 容错 二维表**（实测）：
   | 任务 | A 漂移阶段数 | 任务容错 | b4（无回放） | r3（+回放） | 增益 |
   |---|---|---|---|---|---|
   | A | 3 | 低（交接精度） | 0 | **0** | **+0** |
   | B | 2 | 高（抓滚轮） | 0.57 | **0.80** | **+0.23** |
   | C | 1 | 极低（叠碗对位） | 0 | **0.20** | **+0.20** |
   ⇒ **A 的失效是阈值型**：漂移被回放"减速"但没有"拉回"，残差仍在 A 的容错阈值之外；B 容错大 ⇒ 同样的减速就兑现成 +0.23。

3. **剂量与覆盖面**：replay 梯度占比 ~1/8（every4×w0.5）+ 每任务仅数百关键帧 ⇒ 只约束"buffer 覆盖到的状态点上的输出"，而 A 的失效是全局表征错位。

**零成本验证（把推理变证据）**：
```bash
python vla-scripts/analyze_cl_lora_drift.py \
  --ckpts $LOGS_ROOT/rt_v39_taskA--30000_chkpt \
          $LOGS_ROOT/rt_v39b4_taskD--40000_chkpt \
          $LOGS_ROOT/rt_v39r3_taskD--40000_chkpt --names A1 b4D r3D
```
· r3 的 specific-A cos 明显高于 b4 但 A 成功率仍 0 ⇒ **阈值型失效**成立（机制 2）；· 两者 cos 接近 ⇒ 回放没拦住 A，B/C 的增益只来自输出层正则（机制 3 主导）。

**唯一可能翻盘的设计：bank-aware replay**（回放时把 B_K 装回去）
既然瓶颈是"目标路径从未被训练"，就让回放步骤直接训练它：每个 replay batch 知道自己来自哪个任务的 buffer → 在 replay step 前把 specific-B + block_scale 临时换成**该任务 bank 的版本**、算完梯度再换回。这样 A 收到的梯度对齐到 `(B_K, A)`，正是评估路径。
· 实现面小：4 份 bank 常驻显存（每份 ~11MB；`--bank_film_mode film` 后），swap 只做 `copy_`；
· 预期：A 从 0 抬起的可能性显著高于普通回放（这次是"直接优化目标函数"）；
· 成本：代码改动 + 3 段训练。**待评估，未实现。**

### 10.6 解冻 shared A/B：连续可调的遗忘通道（2026-08 实现）

**现状核对**：stage 2+ 时 **shared A 与 shared B 都是冻结的，且是无条件冻结**（`freeze_stage1_params` → `_freeze_and_reinit_modules`：`module.is_shared` 分支直接 `requires_grad=False`；`freeze_specific_a` 只管 specific 层）。shared 层没有 block_scale（该门控只存在于 specific 层）。stage 1 内 fallback 逻辑会把 `lora_a/lora_b/block_scale` 全部解冻可训，stage 1 结束时再统一冻 shared。

**新增开关**（`12adedd`）：`--freeze_shared False` + `--shared_lr_scale <float>`（独立优化器参数组，与 `film_lr_scale` 同机制）。注意 `reinit_bank_for_new_task` 也已串上 `freeze_shared`，否则刚解冻会被它冻回去。

**机制（与 FiLM/specific-A 漂移的本质差异）**：
1. **shared 层不进 bank**（`save_task_bank` 只存 specific 的 lora_b/block_scale + FiLM）⇒ 解冻 = **没有任何恢复路径**；
2. shared 通路被**所有任务共用** ⇒ 漂移**同时**污染全部旧任务（不像 specific-A 漂移只伤旧任务、且是阈值型）；
3. 损伤是**加性、平滑**的 ⇒ `shared_lr_scale` 给出**连续可调的遗忘曲线**（补上"0.2~0.5 中间残留不可得"缺的那条通道）；
4. 副作用：新任务 D 可能**变好**（共享通路拿到更多容量）。

**⚠️ 实验设计警告（必须遵守）**：解冻 shared = **拆掉 CL-LoRA 的核心保护**。它可以作为"**逐项消融保护机制**"表里的一行（− shared freeze），**不能**当作"CL-LoRA 无回放基线"来讲——评审会问"为什么把共享层解冻"。**已有的合法遗忘阶梯**（无需新训练）：

| 臂 | 保护机制 | 结果 | 保留率均值(A/B/C) |
|---|---|---|---|
| 普通 LoRA | 无（顺序覆盖，无 bank） | 0-0-0-0.82 | 0 |
| b4 | shared 冻结；specific-A **漂移** | 0-0.57-0-0.85 | 0.19 |
| b3 | shared+A 冻结；**FiLM 漂移** 0.2× | 0.26-0.88-0.72-0.82 | 0.62 |
| b5 | 全冻结 | A/B >0.9 | ~0.9 |

**脚本**：`run_v39b7_shared_unfreeze.sh`
```bash
bash run_v39b7_shared_unfreeze.sh                        # 剂量 0.1（温和）→ rt_v39b7_s01_*
SHARED_LR_SCALE=0.3 bash run_v39b7_shared_unfreeze.sh    # 中剂量 → rt_v39b7_s03_*
SHARED_LR_SCALE=1.0 bash run_v39b7_shared_unfreeze.sh    # 全速（应接近普通 LoRA 的遗忘）
USE_REPLAY=True bash run_v39b7_shared_unfreeze.sh        # 回放孪生臂（同剂量 + 原型回放）→ rt_v39r7_*
```
**最有价值的用法不是"造弱基线"，而是 `v39r7`**：测**回放能否替代 shared 冻结**（回放梯度会流进 shared A/B）——若能保住，就支持"回放 = 换取组件自由度"的叙事，这是一条对方法有利的正面结论。

### 10.7 specific-A "冻结量"旋钮：两条路线（2026-08 实现）

> **标签纠正**：**b3 = FiLM 漂移 + specific-A 冻结**（A=0.26 来自 FiLM 失配，γ=1 可救）；
> **b4/r3 = 冻结 FiLM + specific-A 漂移**。A 恒为 0 的痛点在 **b4/r3** 这条线，不在 b3。

**路线①部分解冻**（`--specific_a_lr_scale`，独立优化器组，含 action head 的 A）：
预期不高 —— **Adam 的漂移量 ≈ lr × 步数**（不是 ∝ 梯度大小），缩放 0.2× 只是把漂移减速 5 倍；直接证据：FiLM 用 0.2× 跑 3 个 stage 的 A=0.26，与 1.0× 的 0.27 几乎一致 ⇒ **"温和缩放"大概率仍跨阈值**。要落回阈值内可能得 ≤0.02（那时 A≈冻死=b5）。用 **1 段探针**先验证，别花 6 段训练。

**路线②A 入 bank**（`--bank_save_specific_a True`）—— **推荐**：A 全速训练（保留可塑性），bank 每任务额外存 A_K ⇒ 评估时 **A_K + B_K 同时恢复**（`load_task_bank` 已支持 lora_a）⇒ 配对精确复原 ⇒ 保留率≈自评。代价每任务 +~11MB（与已有 B 载荷同量级；`--bank_film_mode film` 已把 bank 从 2.4GB 压到 ~0.2MB）。
科学价值：把"**冻结 A（正交子空间保护）**" vs "**每任务 A 快照（参数隔离）**"变成两种可比范式。

**⚠️ 继承 bank 的坑（已在脚本里自动处理）**：链式训练中 `task_1_bank.pt` 是从 stage-1 ckpt（`rt_v39_taskA`）**继承**的，由旧代码写出 ⇒ **不含 lora_a** ⇒ 评估任务 A 仍会用漂移后的 A_final（A 照样崩）。评估前必须补：
```bash
python vla-scripts/patch_bank_specific_a.py --ckpt-dir $CKPT_D --src-ckpt $LOGS_ROOT/rt_v39_taskA--30000_chkpt \
    --vla-step 30000 --ah-step 30000      # 从 cl_lora_adapter.pt + action_head--30000.pt 取 specific-A
```
（`run_v39b8_speca_knob.sh` 在 `A_IN_BANK=True` 时会在评估前自动调用它。）

**脚本**：`run_v39b8_speca_knob.sh`
```bash
# ① 便宜探针（只训 B，1 段）: 测该剂量下 1 个 stage 是否已跨阈值
STOP_AFTER_STAGE=2 SPEC_A_LR_SCALE=0.05 bash run_v39b8_speca_knob.sh
# ② A 入 bank（直接达成目标）
A_IN_BANK=True bash run_v39b8_speca_knob.sh
# ③ A 入 bank + 原型回放（回答"A 都不用冻了，回放还需要吗"）
A_IN_BANK=True USE_REPLAY=True bash run_v39b8_speca_knob.sh
```

### 10.16 v41 首战（1 个 stage，B ckpt）：A=0.375 / B=0.94 —— **FiLM 漂移的"第一刀"被量化出来**

**配置**：shared 冻结 ｜ LLM specific-A（L24-31）**放开** ｜ 动作头 A **冻结** ｜ **FiLM 漂 0.2×** ｜ 无回放 ｜ 只训到 B。

**与 n6 的单变量差（同为 1 个 stage、同为 B ckpt、同为动作头 A 冻结）**：

| 臂 | LLM-A | FiLM | Task A | Task B |
|---|---|---|---|---|
| v39b9_n6 | 6/8 层全速漂 | **冻结** | **0.9643** | 0.97 |
| **v41** | 8/8 层全速漂 | **漂 0.2×** | **0.375** | **0.94** |

⇒ **加一个 stage 的 FiLM 漂移（0.2×）就把 A 从 0.964 打到 0.375（−0.59）**，而 LLM-A 漂移本身几乎无害 ⇒ **FiLM 漂移是 A 上"回放够得到"的主损伤通道**（与 §10.15 的判据一致）。
与 b3（FiLM 漂 0.2×、3 个 stage，A=0.26）对照 ⇒ FiLM 损伤**前载且饱和**：1 个 stage 已兑现大部分损失（0.375），再漂两轮只再降 ~0.11。

**同时**：B=0.94（新任务无影响）；A=0.375 **已落在"无回放低基线"的目标区（≤0.4）**。
⇒ **v41 的无回放臂天然满足"低基线"，且它保留的漂移源（FiLM）正是回放够得到的那个** ⇒ 这是构造"无回放低 ↔ 回放高"对比的正确载体。

**续训与孪生臂**：
```bash
# ① 续训 C/D 并全任务评估（B 自动 SKIP）→ tag v41D
STOP_AFTER_STAGE=4 TRAIN_LAYERS="24-31" FREEZE_AH_A=True \
  FREEZE_FILM_STAGE2=False FILM_LR_SCALE=0.2 BANK_FILM_MODE=film TAG=v41 SKIP_B_GATE=1 \
  bash run_v39b9_layermask.sh 2>&1 | tee train_v41_CD.log

# ② 回放孪生臂（唯一差异=回放；需全链重训）→ tag v41_r
STOP_AFTER_STAGE=4 TRAIN_LAYERS="24-31" FREEZE_AH_A=True \
  FREEZE_FILM_STAGE2=False FILM_LR_SCALE=0.2 USE_REPLAY=True BANK_FILM_MODE=film TAG=v41 SKIP_B_GATE=1 \
  bash run_v39b9_layermask.sh 2>&1 | tee train_v41r.log
```
**判读**：若 ② 的 A ≥0.6 而 ① 的 A ≤0.3 ⇒ 单变量不对称成立，主结果拿到；若 ② ≈ ① ⇒ 回放连 FiLM 也拉不住 ⇒ 只剩 bank-aware replay 一条路。

**D ckpt 完整行（2026-08 实测）**：**A=0 / B=0.78 / C=0.54 / D=0.88**（50ep，γ=0，bank 满恢复）

| 任务 | B ckpt（1 stage） | D ckpt（3 stage） | 该任务后续承受的漂移 stage 数 |
|---|---|---|---|
| A | **0.375** | **0** | 3 |
| B | 0.94（新任务） | **0.78** | 2 |
| C | — | **0.54** | 1 |
| D | — | **0.88** | 0 |

**⚠️ 结论修正（重要）**：此前据 n6（1 个 stage，A=0.964）写过"LLM specific-A 漂移无害"——**只在 1 个 stage 成立**。v41 里动作头 A 是**冻结**的，A 仍然从 0.375（1 stage）掉到 **0（3 stage）** ⇒ **LLM-A 漂移会随 stage 累积**（Adam 位移 ∝ lr√N，3 个 stage 足以把 8 层 A 打成与 bank 里 `B_A` 不匹配）。
准确表述应是：
- **单位 stage 的破坏力**：动作头 `fc1` 的 A ≫ LLM-A（v40_fc1：1 stage 即 0 vs n6：1 stage 0.964）；
- **累积效应**：LLM-A 在 **3 个 stage** 后同样足以让最老任务归零（v41 实测）⇒ "LLM-A 可自由解冻"这条**只在短链/少 stage 时成立**。

**为什么 B/C "还是偏高"（0.78 / 0.54）——三条都可解释，且不是异常**：
1. **recency 效应（主因）**：B 只承受 2 个后续 stage、C 只承受 1 个 ⇒ 它们的 FiLM 失配剂量天然更小（与 b3 的 0.26/0.88/0.72 同一形状）；
2. **FiLM 损伤前载且饱和**：0.2× 漂一个 stage 就兑现大部分（v41 1-stage A=0.375 ≈ b3 3-stage A=0.26），所以"加大 FiLM 力度"对 B/C 几乎没用（b3≈b6 已证）；
3. **任务容错**：B（抓滚轮）容错大；C（叠碗）精度敏感，所以 C 掉得比 B 多（0.88→0.54 vs 0.94→0.78）。

**想合法压低 B/C 的三条杠杆（按推荐度）**：
1. **延长任务序列（加 task E/F）**：B/C 变成"更老"的任务 ⇒ 自然下降，且附带"保留率 vs 任务年龄"曲线；两边都讲得通；
2. **提高 LLM-A 漂移速率**（`--specific_a_lr_scale 2.0`，仍是**回放够得到**的通道 ⇒ 不破坏不对称）：该通道**不饱和**（1→3 stage 已见 0.964→0），比加 FiLM 力度有效；
3. 增加每 stage 步数（同 2 的效应，但更笨）。
**不要**用不可达通道（动作头 A / shared）压 B/C——那会让回放臂一起崩（r3 / b7-r7 已证）。

**对主对比的影响（其实不大）**：主差异仍由 **A** 承载（v41: 0 ↔ v41_r: ?）。若回放把 A 抬到 0.5-0.7，旧任务均值即从 (0+0.78+0.54)/3=**0.44** 升到 (0.6+0.9+0.8)/3≈**0.77**，gap ≈0.33 —— 足以成文；B/C 偏高并不妨碍结论，只要两条臂同口径报告。

### 10.15 如何合法构造"无回放低 ↔ 回放高"的对比（对比度只能来自**回放够得到的通道**）

**用户提出的关键顾虑**：路线 (a)（冻结/bank 关键 A）会让**两条臂一起涨**，回放臂达预期但无回放臂 retention 也变高 ⇒ 叙事崩。
**这个顾虑是对的**，它把一个设计原则逼出来了：

> **对比度不能来自"被保护的组件"（那两边一起受益），只能来自"回放梯度够得到、但没有被保护"的组件。**

**判据表（决定一个漂移源能否用来造对比）**：

| 漂移源 | 无回放会掉吗 | 回放够得到吗 | 能否造对比 |
|---|---|---|---|
| 动作头 `fc1` 的 A | ✅✅（1 阶段即 0） | ❌（评估用 `(B_K, A)`，`B_K` 训练时从不加载） | **❌ 两边都 0**（r3 实证） |
| shared A+B | ✅✅（1 阶段即 0） | ❌（1/8 权重拉不住，b7/r7 = 0↔0） | ❌ |
| **FiLM 漂移** | ✅（3 阶段 A=0.26；B/C 0.88/0.72） | ✅ **很可能**（输入通路上，回放直接约束特征） | **✅ 首选**（b3 ↔ v39r5） |
| **LLM specific-A** | ⚠️ 弱（1 阶段 0.964） | ✅（r3 里 B 0.57→0.90 就是靠它） | ✅ 叠加用 |
| **bank-aware replay**（改回放本身） | —（无回放臂不受影响） | ✅ 直接训练 `(B_K, A)` | **✅✅ 天然单边** |

**由此得到正确配方（两步，缺一不可）**：
1. **保护"回放够不到的致命通道"（动作头 `fc1` 的 A）** —— 这不是"给两边送分"，而是**把地板从 0 抬起来、制造出对比空间**；两边都 0 时什么都证明不了；
2. **保留至少一个"回放够得到"的漂移源**（FiLM 漂移 / LLM-A 可训练）—— **不对称正是在这里产生的**：无回放臂掉，回放臂靠复习拉住。

⇒ **`v41` 恰好就是这个配方**（动作头 A 冻结 + LLM-A 放开 + FiLM 漂 0.2×），且其**无回放臂已在跑** ⇒ 下一步只需补 **`v41_r` = v41 + 原型回放**（3 段训练），即得干净单变量对。

**关于"B/C 要 <0.5"**：**不建议当硬指标**。因为能让 B/C 掉到 0.5 以下的通道（动作头 A、shared）回放都够不到 ⇒ 一旦启用，回放臂一起崩（r3 的 A=0、b7/r7 的 0↔0 已证）。**合法压低整体 retention 的办法是延长任务序列**（5~6 个任务时 B/C 变成"更老"的任务，自然进入低位，而回放臂仍能靠复习保住）——这是唯一两边都讲得通的做法。

**执行优先级**：
1. `v41_r`（v41 + 普通原型回放）—— 最省，先看标准回放能否兑现不对称；
2. `v41` + **bank-aware replay**（只改回放臂的训练方式，无回放臂完全不受影响 ⇒ **天然单边**）—— 期望 gap 最大；
3. 需要整体压低 ⇒ 延长序列（加 task E/F）；
4. **不要**把路线 (a)（冻结/bank）当作"方法"来讲——它抬两边，只适合作"上限/机制消融"行。

### 10.14 定论：r3 相对 b4 提升有限的根本原因（RoboTwin，不再依赖 LIBERO 对照）

> **一句话**：**回放的梯度作用在一条"只在训练时存在"的参数路径 `(B_current, A)` 上，而评估用的是 `(B_K, A_final)`——这条路径从未被训练过**；而 b4/r3 里决定旧任务成败的参数恰好是**动作头入口投影 `fc1` 的 A**（bank 不存、评估只能用漂移版、回放只能隔着 `B_current` 间接影响的那一个）。⇒ 这不是"回放强度不够"，而是**优化目标与评估目标的结构性错位**，加权重/加频率治不了。

**证据链（全部 RoboTwin 实测）**：
1. **结构事实**：`save_task_bank` 存 specific-B/gate（+FiLM），**不存任何 A**；训练全程不 load bank ⇒ 评估合成 `(B_K, A_final)` 从未被优化；
2. **致命点定位**：v40_fc1 —— LLM-A 全冻、**只让动作头 `fc1` 的 A 漂 1 个 stage** ⇒ **A=0**；对照 n6 —— **6/8 层 LLM-A 全速漂 1 个 stage** ⇒ A=0.964 ⇒ 破坏集中在动作头的 `fc1`（无稀释的末端适配器）；
3. **不是剂量问题**：全链累计回放 batch ≈ **A 18,300 / B 6,700 / C 5,000**（按 D 阶段 A×2+B×1+C×3 加权与 round-robin 计）——**A 拿到最多回放，收益 +0**；旁证 v39r（every1/w1.0）反而压垮新任务（D=0.02）；
4. **剂量 × 容错**解释各任务差异：A 需撤销 **3 个 stage** 的 fc1-A 漂移（超出能力，+0）；B 需 2 个 stage 但**容错大**（+0.33）；C 只 1 个 stage 但**精度敏感**（+0.20）；
5. **反面对照**：评估路径上没有任何漂移参数时（b5 全冻）**无需回放**即 >0.9；shared 漂移（b7/r7）时回放也救不回（0 vs 0）⇒ 回放的有效区间被夹在"**漂移参数可被回放梯度触及 + 任务容错足够**"的窄带内。

**推论（可执行）**：
- 想提升保留率只有两条路：**(a) 让评估路径可恢复/不漂**（bank 存 A，或冻结 `fc1` 的 A）；**(b) 让回放直接训练评估路径**（bank-aware replay：回放 step 前把该任务的 `B_K` 装回模型）；
- **不必冻全部 A**：只需保护动作头 `fc1` 的 A（≈0.92M 参数）；LLM 侧 L24-31 可放开（n6 / v40_llmonly 1 阶段 0.964）；
- 论文口径：RoboTwin 上**不能写"回放救遗忘"**，能写的是"**回放的有效性取决于评估路径的可达性**"+"**bank/冻结等结构保护才是 retention 主机制**"。

**可证伪点**：若实现 bank-aware replay 后 A 仍为 0 ⇒ 上述"路径错位"诊断不成立，应回到"漂移量级/任务容错"解释。

### 10.13 跨平台核对：**LIBERO 的 V47 + 回放是成功的**（0.76/0.98/0.92/1.00），RoboTwin 的 r3 却是 0 —— 差异在**评估协议**

> ⚠️ **修正（用户提供的一手数据）**：LIBERO **V47（`first_lora_layer=20` + `freeze_specific_a=False`，specific-A 漂移）**：
> **无回放 = 0 / 0.3 / 0.6 / 1.0；+ 原型回放 = 0.76 / 0.98 / 0.92 / 1.00** ⇒ **回放把 A 从 0 救到 0.76，LIBERO 上是成功的**。
> 此前本文档据 `方法论.md §9.3` 一句歧义表述（"V47 A 仍崩 0.00"）误判为"A 漂移 + 回放也失败"——**该句应理解为 V47 的无回放/中间态，`方法论.md §9.3` 需更正**。

**核心对照（A 漂移 + 原型回放，两个平台）**：

| | A | B | C | D |
|---|---|---|---|---|
| **LIBERO V47** 无回放 | 0 | 0.3 | 0.6 | 1.0 |
| **LIBERO V47 + 原型回放** | **0.76** | **0.98** | **0.92** | **1.00** |
| **RoboTwin b4** 无回放 | 0 | 0.57 | 0 | 0.85 |
| **RoboTwin r3 + 原型回放** | **0** | 0.90 | 0.20 | 0.88 |

⇒ **回放在 LIBERO 上能救 A（+0.76），在 RoboTwin 上完全救不动（+0）** —— 这是真实差异，必须解释。

**首要假设：评估协议不同 —— "动作头是否按任务从 bank 恢复"**

| | LIBERO（V38/V39/V47） | RoboTwin（deploy_policy.py） |
|---|---|---|
| 调用 | `_load_task_bank(model, **None**, bank_path)` ⇒ **动作头从不恢复**（`方法论.md:718` 已记录的 eval 端 caveat） | 传入 `action_head` ⇒ **每个任务恢复自己的动作头 B** |
| 评估时的动作头 | **当前模型的（最新任务的）那一份，所有任务共用** | 各任务自己的 `B_K`（与 `A_final` 配对） |
| ⇒ 回放训练动作头 B 的效果 | **直接作用在评估路径上**（评估用的就是这份共享头）⇒ 回放把它练成"多任务头" ⇒ 所有旧任务一起受益 | **被 bank 覆盖掉** ⇒ 回放对动作头的改进全部作废，只能经 A 间接起作用 ⇒ 抬不动 A |

**这一条假设同时解释四个现象**：
1. **LIBERO V38（A 冻结）无回放 A=0.44 → +回放 0.96（+0.52）**：不是"回放防遗忘"，而是**回放把共享动作头练成了多任务头**；
2. **LIBERO V47（A 漂移）+回放 A=0.76**：同一条退路救了 A；
3. **RoboTwin b5（A 冻结、无回放）A/B >0.9**：动作头**被正确恢复**，本就不需要"共享多任务头"这条退路；
4. **RoboTwin r3（A 漂移）+回放 A=0**：动作头被 bank 覆盖 ⇒ 回放只能经 A 起作用，而 A（动作头 `fc1`）的漂移无法被这种间接信号拉回。

**次要差异（真实存在，但不是主因）**：每 stage 步数（RoboTwin 30k/40k vs LIBERO 12k/20k，Adam 位移 ∝ √N）、动作维度 7D vs 14D（动作头输入 4096×7 vs 57344）、任务差异度（LIBERO A/B 同属 `libero_spatial`；RoboTwin 跨域）、回放强度与 buffer 预算（LIBERO 的 every/weight/KD 待核对）。

**判定：既不是 RoboTwin 的 bug，也不是"回放无效"** —— 是**评估协议决定了"回放能不能作用在评估路径上"**：
- **恢复动作头（RoboTwin）** ⇒ 回放只能修 A ⇒ A 漂移时几乎无效；
- **不恢复动作头（LIBERO）** ⇒ 动作头成了**共享可训练组件** ⇒ 回放直接优化它 ⇒ 效果巨大。
**RoboTwin 的独立贡献**：把"该冻哪一部分"从"全部 specific-A"精确到**动作头的 `fc1`**（v40_fc1：只放它一个 ⇒ A=0；只冻它一个 ⇒ A≈0.98）⇒ 得到比 V38/V48 更宽松的 **v41**。

**两条零训练成本的验证（决定性）**：

① **在 RoboTwin 上复现 LIBERO 协议**（动作头不恢复 = 保留当前模型的；`restore_scope="llm"` 即"只恢复 LLM 侧"）：
```bash
cd /mnt/data/pengshengdi/RoboTwin-main
for ck in rt_v39b4_taskD rt_v39r3_taskD; do
  BANK_RESTORE_SCOPE="llm" FILM_GAMMA=0 bash policy/openvla-oft/eval_sequence.sh \
    $LOGS_ROOT/${ck}--40000_chkpt 4,4,5,5,6,6,7,7 30 "${ck}_llmonly" A B C D
done
```
预测：**r3 的 A 明显 >0（0.3~0.6），b4 仍 ≈0**（只有回放把那颗共享动作头练成了多任务头）⇒ 若成立，**"共享动作头 + 回放"在 RoboTwin 上同样可行**，是一个新的可选设计。

② **在 LIBERO 上改用修正后的评估**（把 `action_head` 传进 `_load_task_bank`）重评 V47 的 +回放 ckpt：预测 A 从 0.76 明显下滑 ⇒ 证实"LIBERO 高分依赖共享动作头"。两条一起做，平台差异即被钉死，论文里也能明确写出口径。

### 10.12 r3 完整行（0 / 0.90 / 0.20 / 0.88）与 b4 对照：**回放的收益为何只在 B 上体现**

**数据（同一配置，唯一差异=原型回放；γ=0；r3 的 D=30ep 口径）**：

| 任务 | A 漂移剂量（评估时 A 距该任务训练时 A 的 stage 数） | b4（无回放） | r3（+原型回放） | 回放增益 | 任务容错 |
|---|---|---|---|---|---|
| A | **3**（B/C/D 三个阶段都在漂） | 0 | **0** | **+0** | 低（交接精度） |
| B | 2 | 0.57 | **0.90** | **+0.33** | **高**（抓滚轮） |
| C | 1 | 0 | **0.20** | **+0.20** | **极低**（叠碗对位） |
| D | 0（新任务） | 0.85 | **0.88** | +0.03（噪声） | — |

**三条机制（共同解释这个形状）**：

1. **回放优化的是"另一条路径"**（结构性主因）：评估任务 K 用的合成是 `(B_K(来自 bank), A_final)`；而训练时模型里装的是 `(B_current, A)` —— **B_K 从不参与训练**（它躺在文件里）。所以回放**从未直接优化过评估时真正使用的那条路径**，只能"间接"把 A 推向对旧 B_K 更友好的区域。
2. **剂量 × 容错**：A 需要撤销 **3 个 stage** 的动作头 A 漂移 ⇒ 远超可修正范围（+0）；B 只需 2 个 stage 且**容错大** ⇒ 间接修正就足以兑现（+0.33）；C 剂量最小（1 个 stage）但**容错极低** ⇒ 只能抠回 0.20。
3. **回放暴露量不是瓶颈**：按 D 阶段 buffer 加权 A×2+B×1+C×3 与 round-robin 计数，全链累计回放 batch 约 **A≈18300 / B≈6700 / C≈5000** —— **A 拿到的回放最多却毫无提升** ⇒ 说明瓶颈不是"复习不够"，而是**要修正的量级太大 + 优化路径错位**。

**结论**：
- 回放能兑现的是"**粗粒度行为**"（容错大的任务），**修不了精度型任务，也修不了最老的任务**；
- `(B_K, A)` 这条路径只要没被训练过，回放的天花板就很低 ⇒ **唯一的结构性正解是 bank-aware replay：回放时把对应任务的 B_K 装回模型**（让梯度作用在评估路径上）；
- 对照：**不要 A 漂移**（b5 全冻 / v41 只放 LLM-A）时回放甚至无事可做 ⇒ 回放的价值区间被夹在"轻度错位 + 有 bank 兜底"之间。

### 10.11 shared 层漂移 + 回放（b7/r7）：**1 个 stage 就把旧任务打到 0，回放救不回**

**实测（`STOP_AFTER_STAGE=2`，即只训 B 一个 stage，shared A+B 以 lr×0.3 漂移，A 用 bank 满恢复评估）**：

| 臂 | shared A/B | 回放 | Task A | Task B |
|---|---|---|---|---|
| **v39b7** | lr×0.3 漂移 | 无 | **0** | 0.96 |
| **v39r7** | lr×0.3 漂移 | 原型 every4/w0.5 | **0** | 0.94 |

**结论**：
1. **shared 漂移是致命的**（比 specific-A 漂移更狠）：n6 里 LLM specific-A 6 层全速漂 1 个 stage，A 仍 0.964；而 shared 只以 0.3× lr 漂 1 个 stage，A 就归零。原因：shared 分支是 stage 1 为任务 A 学到的**唯一"已提交"适配**，且**完全不在 bank 里**（无任何恢复路径）。
2. **回放救不回**（0 vs 0），且新任务还略降（0.96 → 0.94，回放稀释）。机制：回放梯度只占 ~1/8（every4×w0.5），而 Adam 下 shared 的 40k 步位移 ≈ `0.3×5e-4×√40000 ≈ 0.03`，已超过参数自身尺度 ⇒ task loss 主导，回放拉不住。
3. （旁证）r7 的 B 比 b7 低 0.02 —— 与"回放稀释新任务"的既有观察一致（b5 0.9x vs v39r2d 0.80）。

**四种漂移源的统一表（1 个 stage；A 用满恢复评估）**：

| 漂移源 | 旧任务 A | 回放能否救 | 有无 bank 兜底 |
|---|---|---|---|
| LLM specific-A（L26-31 全速） | 0.964 | — | ✅ 有（B_K 在 bank） |
| 动作头 `fc1` 的 A | **0** | 未测 | ✅ 有（但配对已断） |
| **shared A+B（lr×0.3）** | **0** | **❌** | **❌ 无** |
| FiLM 漂移 0.2×（3 阶段） | 0.26（b3） | 未测（v39r5） | ✅ 有（γ=1 可恢复） |

⇒ **规律**：**"评估路径上存在漂移、且无 bank 兜底的参数" ⇒ 旧任务归零，且 1/8 权重的回放补不回来**。回放的作用域被压缩到"**有 bank 兜底、但配对/输入被扰动**"的那一类（specific-A 漂移时 B +0.23/C +0.20；FiLM 漂移时待测）。

**对"无回放低基线 ↔ 回放高保留"对照的影响**：无法用 shared 漂移造出该对照（两边都是 0）。剩余唯一有 favorable prior 的单变量对是 **b3（FiLM 漂 0.2×, 无回放, A=0.26）↔ v39r5（同配置+原型回放）**——旁证：proprio-bug 时代的 v39（A=0）↔ v39r2（+回放, A=0.69）说明回放**能**减缓 FiLM 漂移。

### 10.10 关键翻转之二（2026-08）：**"部分恢复 bank" ≠ "部分记忆"** —— 未恢复部分是"新任务的适配器"，造成任务身份冲突

**实测（v39b9_n6 的 B ckpt：LLM 侧 L26-31 的 A 已漂 1 个 stage，动作头 A 冻结；满恢复时 A=0.9643）**：

| restore_scope | 恢复张量 | 未恢复部分的实际取值 | Task A |
|---|---|---|---|
| `all` | 120（全部） | — | **0.9643** |
| `action_head` | 8 | LLM 侧 112 个 = **任务 B 的 B** | **0** |
| `action_head+layers:28-31` | 64 | LLM L24-27 的 56 个 = **任务 B 的 B** | **0** |

**机制**：`load_task_bank` 默认语义里，**未恢复参数保持"当前 checkpoint 的值"** —— 那不是"记忆缺失"，而是**"新任务（B）的适配器仍然生效"**。部分恢复 ⇒ **半个网络执行 A、半个网络执行 B** ⇒ 任务身份冲突 ⇒ 比"要么全 A 要么全 B"都差。**这条轴上不存在可调中间残留**。

**与"漂移侧"结论不矛盾（问的是不同问题）**：
- **漂移侧**（训练时谁漂）：A 漂 + B_K 已恢复 = "对的 B 配错的 A"，属**同一身份内的扰动** ⇒ 可容忍（n6：6 层 A 全速漂，A=0.9643）；
- **恢复侧**（评估时谁没恢复）：未恢复层的 B 停在**新任务的 B** ⇒ **另一个任务身份**同时在驱动 ⇒ 致命。
⇒ **bank 必须整体原子恢复（task identity switch）**，不能"恢复一部分"当旋钮。

**正确旋钮 = `@zero` 语义**（未恢复部分置零 ⇒ 该分支无贡献 = 干净的记忆缺失）：
```bash
CKPT=$LOGS_ROOT/rt_v39b9_n6_taskB--40000_chkpt TAGP=n6 bash run_v39_bank_scope_sweep.sh
# 默认扫: all / action_head@zero / action_head+layers:28-31@zero / ...26-31@zero / ...24-31@zero / action_head
```
· `all` 应复现 0.9643（顺带校验 scope 机制）；· `...+24-31@zero` ≡ `all`（8+112=120）；· **只有 `@zero` 家族可能出现中间值**——那才是能进论文的"记忆量-保留率"曲线。

**实测 `action_head+layers:28-31@zero`（恢复 64 / 置零 56）：Task A = 0.1，且 Task B 明显下降** —— 两个数都有明确解释：

1. **A=0.1**：置零 L24-27 的 B ⇒ 任务 A 的那 4 层适配被**移除**（不是"被 B 覆盖"，而是"没有"）⇒ A 只剩 0.1。
   对比：`all`（120/120）= 0.9643、覆盖 64/120 = 0.1 ⇒ **覆盖轴上同样是"近乎全或无"**。
   ⚠️ 注意这里混了两种效应：既"少了 4 层记忆"，又"把训练时存在的 4 条分支从中途掐断"（下游层是按它们存在训练的）⇒ 置零不是"部分记忆"的完美代理，结论表述要留余地。
2. **Task B 下降（必然，不是 bug）**：对**最新任务**来说，`task_2_bank.pt` 就是它**自己的适配器**（stage 2 结束时存的）。所以：
   - **不加 `@zero`** 的部分恢复对最新任务是**空操作**（未恢复 = 它自己的值）⇒ B 保持 ~0.91 ✓（此前实测）；
   - **加 `@zero`** ⇒ 等于**把它自己 L24-27 的适配器删掉** ⇒ B 自然掉分。
   ⇒ **scope 旋钮对"最新任务"和"旧任务"的作用完全不同**：对旧任务它是"记忆覆盖/缺失"，对最新任务它是"自残"。

**正确的测量协议（记忆覆盖曲线）**：
- 覆盖率曲线**只读旧任务的数**（`TASKS="A"`，或 C/D 里的旧任务）；最新任务另测；
- 最新任务的干净口径是 **`eval_task_id=0`**（完全不加载 bank = 纯当前权重自评），或用 `all` 口径；
- 每个 scope 都应与 `all` 同口径对照，不要跨口径比较。

### 10.9 关键翻转（2026-08）：归因错了 —— **罪魁是"动作头的 specific-A"，不是 LLM 的 L24-31**

**层掩码探针实测（1 个 stage，A 用 bank 恢复，动作头 A 默认冻结）**：

| 臂 | LLM 侧解冻层 | 动作头 A | Task A | Task B |
|---|---|---|---|---|
| b5 | 0/8（全冻） | 冻结 | >0.9 | >0.9 |
| v39b9_n2 | 2（L30-31） | 冻结 | 0.8571 | 0.9464 |
| v39b9_n4 | 4（L28-31） | 冻结 | 0.9107 | 0.9286 |
| **v39b9_n6** | **6（L26-31）** | 冻结 | **0.9643** | ~0.97 |
| v39b8_s02（lr×0.2 探针） | **8（全解冻, 降 lr）** | **解冻** | **0/56 = 0** | 0.9107 |
| b4 / r3 | 8（全解冻, 3 阶段） | **解冻** | 0 | 0.57 / 0.80 |

**结论：LLM 侧 L24-31 的 specific-A 即使 6/8 层以全速 lr 漂移 1 个 stage，任务 A 保留率仍 0.96**（甚至比 b5 高，属噪声范围）。
⇒ 此前"specific-A 漂移 ⇒ A 恒为 0"的四次实证（v39/b2/b4/r3）**全部同时解冻了动作头的 A**；lr×0.2 探针同样解冻了动作头 A ⇒ **真正致命的是动作头那 4 个注入 Linear 的 A**。

**为什么动作头 A 的漂移致命而 LLM 侧不致命（机制差异）**：1. **LLM 侧**：specific-LoRA 是**加在巨型预训练残差流上的小修正**（`Wx + s·g·B·A·x`），底层 base 权重冻结且贡献占绝对主导 ⇒ 即使 B_K·A_final 配错，策略只是"略微跑偏"，容错大；且 T=25 的动作 chunk 输出经过多层整流。
2. **动作头侧**：base MLP 在 stage 1 随机初始化后即冻结（不再训练），**LoRA 就是这条通路的全部可学习内容**，而且它是**最终输出级**（没有任何后续层稀释误差）⇒ B_K 与 A_final 一失配，动作映射直接报废 ⇒ 所有任务归零。
3. 这也解释了 r3 的"B +0.23 / C +0.20 但 A +0"：回放能部分修补动作头 A 的漂移（弱），但 A（最老、漂最多）修不回来。

**待验证（两条决定性对照，各 1 段探针）**：
```bash
# ① 全部 8 层 LLM-A 漂移 + 动作头 A 冻结  ⇒ 预测 A ≈ 0.9（高）
STOP_AFTER_STAGE=2 TRAIN_LAYERS="24-31" FREEZE_AH_A=True bash run_v39b9_layermask.sh
# ② LLM-A 全冻 + 动作头 A 漂移          ⇒ 预测 A ≈ 0（崩）
STOP_AFTER_STAGE=2 TRAIN_LAYERS="none" FREEZE_AH_A=False bash run_v39b9_layermask.sh   # → tag v39b9_ahonly
```
若两条都如预测 ⇒ **"必须冻结 specific-A" 应精确改写为 "必须冻结（或入 bank）动作头的 specific-A"**，LLM 侧 L24-31 的 A 可自由漂移换取可塑性 —— 这比"全冻"宽松得多，而且**只需把动作头的 4 个 A 存进 bank（每任务 ≈2.2MB）就能同时高保留+高可塑**。

### 10.8 探针实测（2026-08）：**A 漂移量的容忍度 ≈ 0** ⇒ 路线①判死

**结果（档 0.2，lr=1e-4，只训了 1 个 stage=B）**，在 B ckpt 上评估：

| 任务 | 口径 | 结果 |
|---|---|---|
| **A** | bank 恢复（eval_task_id=1） | **0/56 = 0.0000** |
| B | 当前权重 | 51/56 = 0.9107 |

对照：b5（A 全冻）A>0.9；b4（A 全速漂 3 个 stage）A=0。⇒ **只漂 1 个 stage、lr 只有主干的 1/5，A 就已经彻底归零**。

**定量解释（为什么"降 lr"救不了）**：
- Adam 每步位移 ≈ `lr`（sign-like 更新）⇒ N 步随机游走位移 ≈ `lr·√N`；
- 档 0.2：`1e-4 × √40000 = 0.02`；档 0.05：`2.5e-5 × √40000 = 0.005`；
- 而 specific-A 的初始化尺度（kaiming，fan_in=4096）≈ `1/64 ≈ 0.0156`（RMS ≈ 0.009）；
- ⇒ **0.2 档的位移(0.02) 已经是参数自身尺度的 2 倍** ⇒ cos(A_1, A_2) ≈ 0.4 量级 ⇒ bank 里的 `B_A` 与 A 完全失配 ⇒ A 归零**与步数无关，与"跨阈值"一致**；
- 0.05 档位移 0.005 ≈ 尺度的一半 ⇒ 预测 cos ≈ 0.85，**A 大概率仍接近 0（最多个位数~0.3）**——待实测确认。

**附带事实**：同 ckpt 的 B 自评 = **0.91** ⇒ 解冻 A **不影响新任务学习** ⇒ 在"不把 A 存进 bank"的前提下，解冻 A **既救不了旧任务、也没换来可见的新任务收益**（对该设置而言）。

**结论**：**"减少 specific-A 解冻量"没有可用中间区**（与 FiLM 漂移同为开关式）。要"不冻 A 又高保留"，唯一出路是 **路线② A 入 bank（v39r4）**。

**验证机制的一分钟命令**（量化 1 个 stage 把 A 打散了多远）：
```bash
cd /mnt/data/pengshengdi/openvla-oft
python vla-scripts/analyze_cl_lora_drift.py \
  --ckpts $LOGS_ROOT/rt_v39_taskA--30000_chkpt \
          $LOGS_ROOT/rt_v39b8_s02_taskB--40000_chkpt --names A1 B(0.2) --pair-only
```
预期 `specific lora_a` 的 cos ≈ 0.3~0.5（若实测 cos 很高但仍 A=0 ⇒ 说明失配阈值极低，比"位移/尺度"估计更敏感）。

### 10.5 两条臂各自"达到预期"的配置路线（2026-08 设计）

#### 10.5.0 实测: b5（无回放, 全冻结）= A/B 均 >0.9 ⇒ 完整解释（2026-08, 关键转折）

**结果**：b5（全程冻结 FiLM + specific-A 冻结 + block_scale 冻结 + proprio 修复 + **无回放无 KD**）= **A >0.9、B >0.9**（C/D 待补）
⇒ **高于 v39r2d 的 0.62-0.88-0.88-0.80**（有回放 + KD）。**"回放带来保留率"的叙事被自身基线推翻。**

**为什么 b5 能到 >0.9（构造性）**：A 冻结 ⇒ bank 里的 `B_K` 永远与 `A_final` 精确配对（A 从未变过）；FiLM 从 stage 2 起冻结 ⇒ 评估用的 FiLM = 各任务训练时的 FiLM，**完全对齐**。⇒ **评估路径上没有任何会漂移的参数**，保留率必然高。

**统一框架（解释全部历史数据）**：
> **保留率由"评估路径上是否还有会漂移的可训练参数"决定；回放只在存在这种漂移参数时才起作用。**

| 评估路径上的漂移源 | 无回放 | 有回放 | 回放是否有效 |
|---|---|---|---|
| **无**（A 冻结 + FiLM 冻结，b5） | **A/B >0.9** ✅ | v39v ← 预测 ≈ b5（回放惰性） | ❌ 无事可做：`B_K` 躺在 bank 文件里、A 冻死，**回放梯度碰不到评估路径** |
| FiLM 漂移（A 冻结） | b3 = 0.26/0.88/0.72 | v39r2（1.0×, proprio bug）A=0.69；r2d A=0.62 | ✅ 减缓 FiLM 漂移 → A 从 ~0.26 → 0.62-0.69 |
| specific-A 漂移 | b4 = 0/0.57/0/0.85 | r3 = 0/0.80/0.20 | ✅ 部分（B +0.23、C +0.20），❌ A（漂移剂量过大） |

**"r2d 反而更低"的真正原因 = 两者不是同一配置**（至少 4 个变量）：
① r2d 的 FiLM 在 B/C 阶段以 **1.0× lr 漂移**、只在 D 阶段冻结 ⇒ A 评估时的 FiLM ≠ A 训练时的 FiLM（残留失配 → A 0.62 < 0.9）；
② r2d 的 B/C bank 继承自 v39r2 链（**漂移 FiLM + proprio bug 时代**）；
③ r2d 开了 **KD λ0.2**；
④ r2d 只重训了 D 阶段（b5 全程干净重训）。
⇒ **r2d 应从"方法性能"降级为"脏对照"；方法数字改用 v39v（冻结 FiLM + A 冻结 + 原型回放 + 无 KD + proprio 修复，全程重训）。**

**可检验预测（v39v 一跑就知）**：v39v 的**保留率 ≈ b5**（回放对保留率惰性）；但 v39v 的**新任务 D 可能 ≤ b5**（回放稀释新任务学习）。若两者皆如此 ⇒ "回放在全冻结配置下零增益、甚至略伤新任务"成立。

**叙事升级（建议）**：回放的价值不在"提升保留率"，而在**换取组件自由度**——允许 FiLM（或其它组件）不冻结/漂移，仍把旧任务保留住。核心干净对：**b3（FiLM 漂 0.2×, 无回放）= 0.26-0.88-0.72-0.82 ↔ v39r5（同 + 回放）**。若 v39r5 的 A 明显 > 0.26 ⇒ 该叙事成立且可量化。
**根本张力（必须先认清）**：**回放的"可见价值"与"高保留目标"在当前设计里此消彼长**——
回放只在存在漂移源时显出作用；而漂移源一旦消除（A 冻结 + FiLM 冻结/恢复），无回放本身就已经很高（v39f A/B≈0.7；b3 B/C=0.88/0.72）。所以必须**选一个叙事**，并让两条臂共用同一个遗忘源：

| 叙事 | 遗忘源（两侧相同） | 无回放臂 | 回放臂 | 单变量？ |
|---|---|---|---|---|
| **① 回放卖点** | FiLM 漂移 0.2×（A 冻结） | **b3 = 0.26-0.88-0.72-0.82** ✅ | **v39r5 ← 最高优先** | ✅ 只差回放 |
| ① 强化版 | FiLM 漂移 1.0× | b6 = 0.27-0.82-?-0.875 ✅ | v39r6（可选） | ✅ |
| **② 结构卖点** | 无（全程冻结 FiLM） | **b5 ← 待跑** | **v39v ← 待跑** | ✅ 只差回放 |
| ③ 负面对照（诊断行） | specific-A 漂移 | b4 = 0-0.57-0-0.85 ✅ | v39r3 = 0-0.80-0.20-?（已跑） | ✅ 但**回放救不回**，不能当主叙事 |

**达到"预期"的杠杆（按杠杆大小排序）**：

*回放臂（目标 γ=0：A≥0.5 / B≥0.85 / C≥0.8 / D≥0.8）*
1. **specific-A 冻结（硬前提）** —— 不冻结则 A 恒 0（§10.3 四次实证）；
2. **评估口径 γ 两侧必须一致**：γ=1 会把 A 抬到 ~0.9，但**无回放臂也会同时变高 → 对比消失**；故主表用 **γ=0**（纯结构保留，回放作用最可见），γ=1 只作上限附表；
3. **回放配额**：D 阶段 A×2+B×1+C×3 已偏向最老的 A；若 A 仍低可升 A×3（每轮 A 占比 2/6→3/7）；
4. **回放强度**：every4/w0.5（1/8 梯度）→ every2/w0.5（1/4）。⚠️ 上限警戒：v39r 的 every1/w1.0 曾压垮新任务（D=0.02）；调强后**必须先看新任务自评不掉**；
5. **KD λ0.2**（teacher=上一任务快照）：把输出复习升级为软蒸馏，v39r2d 的 0.62-0.88-0.88-0.80 是"回放+KD"上限参考；方法定义是纯回放，开 KD 需单变量声明；
6. **不要动**：proprio 修复、block_scale 冻结、bank 机制；
7. 最后手段：A 阶段 30000 → 40000 步（补 A 训练量偏少的系统性偏差，成本 1 段 + 重跑整链）。

*无回放臂（目标：保留明显低于回放臂，且新任务 D 正常 ≥0.8）*
1. 遗忘源**只能留一个、且与回放臂同源**：首选 FiLM 漂移 0.2×（b3 已达标：A=0.26 有下降空间，B/C 仍高）；
2. **不要用 A 漂移当基线遗忘源**（b4）——它与回放臂不构成"回放可解"的关系（r3 已证），且构成"拿最弱配置陪衬"的选择性偏差；
3. 保证 proprio 修复生效、block_scale 冻结、步数与回放臂一致；评估用同一 γ。

**判读规则（跑完 v39r5 即知走哪条叙事）**：
- v39r5 的 A ≥ 0.5 或 C ≥ 0.8 ⇒ **叙事①成立**（回放补 FiLM 漂移），b3 可作正式基线；
- v39r5 ≈ b3 ⇒ **叙事②成立**（结构隔离是主机制），必须补 b5-vs-v39v 把"回放的边际价值"如实量化，并按"结构为主 + 回放增强 X"改写论文。

> ⚠️ v39u ↔ v39r2d **差 3 个变量**（回放形式 / KD / FiLM 处理），非干净单变量消融；主变量 = 回放形式，
> KD 与 FiLM 影响预计小（b3≈b6 已证 FiLM 强度无影响）。rt_v39 全系列 freeze_specific_a=True，仅 b4 用 False。

| 项 | 值 |
|---|---|
| 配置 | 冻结 FiLM（stage≥2）+ specific-A 冻结 + block_scale 冻结 + proprio 修复；**USE_KD=False**；replay every4/w0.5；D 阶段 buffers A×2+B×1+C×3；A 复用 rt_v39_taskA（0.98） |
| buffer | `build_uniform_replay_buffer_robotwin.py`：同预算（匹配原型 manifest 行数）+ 同格式 + 唯一差异=均匀时间采样 |
| 脚本 | run_v39u_B.sh（A 门禁 + 建 buffer + 训 B + B 自评）、run_v39u_CD.sh（C/D + 全任务评估） |
| 状态 | B/C 已训完，A/B/C 顺序评估进行中；对照 v39r2d（含 KD）= 0.62/0.88/0.88/0.80 |

## 11. 待办

- [ ] **v39r5（b3 配置 + 原型回放，见 §10.4）← 最高优先**：`bash run_v39r5_driftfilm_replay.sh` —— 单变量测"回放的真实边际价值"，决定论文叙事能否成立
- [ ] **零成本**：b3 的 D ckpt 用 γ=1 重评（`FILM_GAMMA=1 bash .../eval_sequence.sh $LOGS_ROOT/rt_v39b3_taskD--40000_chkpt 4,4,5,5,6,6,7,7 50 v39b3D_g1 A B C D`）→ 得 bank-FiLM 恢复口径
- [ ] **v39v / v39r3 补跑**（见 §10.1）：v39r3 已跑（C 崩，见 §10.3）；v39v（方法本体）仍需跑
- [ ] b5（冻结 FiLM + A 冻结 + 无回放）→ 与 v39v 构成冻结 FiLM 下的回放单变量对
- [ ] v39u 评估（B/C 顺序成功率）→ 与 v39r2d 对比：**回放形式（uniform vs prototype）+ KD（无 vs 有）双重消融**
- [ ] b6 的 C 重评（数据完整性）
- [ ] 可选：v39r2e（漂移 FiLM + 回放）；v39w（uniform + A 漂移，预计崩，可省）；锚定正则（备选）
- [x] 普通 LoRA 对齐版定稿（lora_scope=cl + FiLM恒等 + proprio 共享）→ 0-0-0-0.82
- [x] b6 重评与结论修正；评估链回归修复（118f974）
- [x] **缺口核对（§10.1）：确认"冻结 FiLM + A 漂移 + 回放"与"方法本体干净配置"均为空白格**
- [x] **b3 配置核对（§10.4）：b3 即"CL-LoRA 无回放"本体，其高保留来自结构保护**

## 12. 历史待办（已并入 §11）

- [x] v39r2d 完整数字入册（A=0.62, B=0.88, C=0.88, D=0.80）
- [x] 普通 LoRA 对齐版（lora_scope=cl）训 A + 自评（0.9167）→ BCD 完成（0-0-0-0.82）
- [ ] CL-LoRA + uniform 回放支线（v39u）→ 见 §10/§11（当前主线）
- [x] 方法论.md 三支线对比 §10.8/§10.9（本机维护）
- [ ] 锚定正则 λ 实验（备选，未跑）
